<UserControl x:Class="sqlDB.MainDBControls.dbView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:sqlDB.MainDBControls"
             mc:Ignorable="d" 
             d:DesignHeight="669" d:DesignWidth="931">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="641*"/>
            <RowDefinition Height="28*"/>
        </Grid.RowDefinitions>
        <DataGrid x:Name="dataGrid" HorizontalAlignment="Left" Height="641" VerticalAlignment="Top" Width="931" CanUserSortColumns="True"
  CanUserAddRows="False" CanUserDeleteRows="False" ItemsSource="{Binding }"/>
        <Button x:Name="addBtn" Content="Добавить" HorizontalAlignment="Right" VerticalAlignment="Top" Grid.Row="1" Margin="0 0 10 0" Click="addBtn_Click"/>

    </Grid>
</UserControl>
